import { GameObject } from './GameObject';

export class Enemy extends GameObject {

  constructor(image, x, y, dx, dy, width, height) {
    super(image, x, y, dx, dy, width, height);
  }

  update() {
    
  }

}
